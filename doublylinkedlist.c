#include<stdio.h>
#include<stdlib.h>
struct node
{
struct node *prev;
struct node *next;
int data;
};
void main()
struct node *head ==NULL,*ptr=NULL;
int a,i,item;
printf("Enter the number of node to be created");
scanf("%d",&a);
for(i=0;i<n;i++)
{
new=(struct node*)malloc(size of(structnode));
printf("Enter the data");
scanf("%d,& item);
struct node
{
new->data+item;
new->next=NULL;
new->prev=NULL;
if(head==NULL)
{
head=new;
}
else
{
ptr=head
while(ptr->next!=NULL)
ptr=ptr->next;
ptr->next=new;
new->prev=ptr;
}




 
